'use strict';

/**
 * @ngdoc function
 * @name hduiApp.controller:BarsCtrl
 * @description
 * # BarsCtrl
 * Controller of the hduiApp
 */
angular.module('hduiApp')
  .controller('BarsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
