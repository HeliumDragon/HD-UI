'use strict';

/**
 * @ngdoc function
 * @name hduiApp.controller:NavCtrl
 * @description
 * # NavCtrl
 * Controller of the hduiApp
 */
angular.module('hduiApp')
  .controller('NavCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
