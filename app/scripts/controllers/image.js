'use strict';

/**
 * @ngdoc function
 * @name hduiApp.controller:ImageCtrl
 * @description
 * # ImageCtrl
 * Controller of the hduiApp
 */
angular.module('hduiApp')
  .controller('ImageCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
